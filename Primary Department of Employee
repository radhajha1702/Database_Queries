SELECT
  e.employee_id,
  e.department_id
FROM Employee e
JOIN (
   SELECT employee_id,
          CASE WHEN COUNT(*) = 1 THEN 1 ELSE 0 END AS only_one
   FROM Employee
   GROUP BY employee_id
) t
  ON e.employee_id = t.employee_id
WHERE (t.only_one = 1)
   OR (e.primary_flag = 'Y');
